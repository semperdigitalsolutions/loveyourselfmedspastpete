---
interface Props {
  categories: string[];
  allLabel?: string;
  filterId?: string;
}

const { categories, allLabel = 'All', filterId = 'category' } = Astro.props;
const btnClass = `filter-btn-${filterId}`;
---

<div class="max-w-7xl mx-auto px-6 mb-12">
  <div class="flex flex-wrap justify-center gap-3">
    {categories.map((category, index) => (
      <button
        class:list={[
          `${btnClass} px-6 py-2.5 rounded-full text-sm font-bold transition-all duration-300`,
          index === 0
            ? 'bg-primary text-white shadow-md shadow-primary/20'
            : 'bg-gray-100 text-text-body hover:bg-gray-200'
        ]}
        data-category={category === allLabel ? 'all' : category}
      >
        {category}
      </button>
    ))}
  </div>
</div>

<script define:vars={{ btnClass }}>
  const buttons = document.querySelectorAll(`.${btnClass}`);
  const cards = document.querySelectorAll('[data-category-item]');

  buttons.forEach(button => {
    button.addEventListener('click', () => {
      const category = button.getAttribute('data-category');

      // Update button styles
      buttons.forEach(btn => {
        btn.classList.remove('bg-primary', 'text-white', 'shadow-md', 'shadow-primary/20');
        btn.classList.add('bg-gray-100', 'text-text-body');
      });
      button.classList.remove('bg-gray-100', 'text-text-body');
      button.classList.add('bg-primary', 'text-white', 'shadow-md', 'shadow-primary/20');

      // Filter cards with animation
      cards.forEach(card => {
        const cardCategory = card.getAttribute('data-category-item');
        if (category === 'all' || cardCategory === category) {
          card.style.display = 'flex';
          card.style.opacity = '1';
          card.style.transform = 'scale(1)';
        } else {
          card.style.opacity = '0';
          card.style.transform = 'scale(0.95)';
          setTimeout(() => {
            card.style.display = 'none';
          }, 200);
        }
      });
    });
  });
</script>

<style>
  [data-category-item] {
    transition: opacity 0.2s ease, transform 0.2s ease;
  }
</style>
